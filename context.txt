========================================================================
FINLY — KONTEKST PROJEKTU
Plik podporowy dla pracy z Claude Code
Ostatnia aktualizacja: 2026-07-17
========================================================================

JAK KORZYSTAĆ Z TEGO PLIKU
--------------------------
To jest "źródło prawdy" o aplikacji Finly. Kiedy zaczynasz nową sesję
z Claude Code, wklej lub wskaż ten plik, żeby AI od razu miało pełny
kontekst: co budujemy, dla kogo, jak ma działać i jakie są decyzje.
Aktualizuj go w miarę rozwoju projektu (nowe wątki dopisuj na dole
w sekcji "DZIENNIK WĄTKÓW").


========================================================================
1. CZYM JEST FINLY (w jednym zdaniu)
========================================================================
Finly to prosta, ładna aplikacja do organizowania własnych finansów —
osobisty "portfel", w którym ręcznie zapisujesz wszystkie swoje dochody,
wydatki, aktywa i cele oszczędnościowe, i widzisz w jednym miejscu, jak
rośnie Twój majątek.

WAŻNE: Finly NIE służy do płacenia ani przelewów. To narzędzie do
ŚLEDZENIA i ORGANIZOWANIA, nie do transakcji. Bezpieczeństwo jest więc
prostsze — nie ruszamy niczyich pieniędzy.


========================================================================
2. PROBLEM, KTÓRY ROZWIĄZUJEMY
========================================================================
Ludzie zarabiają z wielu źródeł, ale nie widzą tego "na jednej kartce":
- stała wypłata z pracy (miesięczna),
- nieregularne dochody: premia, wywiad dla firmy, zlecenie, sprzedaż
  czegoś, napiwek, drobny biznes itd.
Te dochody są rozproszone i trudno ogarnąć, ile się naprawdę zarabia,
ile zostaje i jak blisko jesteś do celu (np. na iPada).

Finly zbiera to wszystko w jednym miejscu — jak uproszczona księgowość,
zrozumiała dla każdego.


========================================================================
3. DLA KOGO (grupa docelowa)
========================================================================
Dla KAŻDEGO, kto zarabia — niezależnie od wieku i skali dochodów.
Aplikacja ma być tak prosta, żeby korzystał z niej zarówno dorosły
z etatem, jak i nastolatek uczący się finansów.

Persona 1 — "Dorosły z wieloma źródłami dochodu":
  ma etat + nieregularne zlecenia i premie, chce widzieć realny obraz
  swoich finansów i odkładać na konkretne cele.

Persona 2 — "Młody uczący się finansów" (np. 12-latek):
  sprzedaje lemoniadę, zarabia np. 100 zł/tydzień, chce wpisywać
  dochody i wydatki i uczyć się zarządzania pieniędzmi na małą skalę.

Wniosek dla UX: interfejs musi być banalnie prosty, przyjazny,
bez żargonu księgowego. "Księgowość dla każdego".


========================================================================
4. FUNKCJE (zakres produktu)
========================================================================
FUNKCJE RDZENIOWE (must-have w MVP):
- Dodawanie DOCHODÓW: kwota, data, kategoria/źródło (np. "wypłata",
  "premia", "zlecenie", "sprzedaż"), notatka. Dochód może być stały
  (cykliczny) lub jednorazowy.
- Dodawanie WYDATKÓW: kwota, data, kategoria, notatka.
- Widok PODSUMOWANIA (dashboard): saldo, suma dochodów, suma wydatków,
  ile zostało w danym miesiącu.
- CELE oszczędnościowe: nazwa celu, kwota docelowa, (opcjonalnie) link
  do produktu (np. iPad) i zdjęcie, pasek postępu ile już zebrane.
- AKTYWA: lista rzeczy/wartości, które posiadasz (gotówka, oszczędności,
  sprzęt, ewentualnie inwestycje), z możliwością ręcznej aktualizacji
  wartości i podglądem, jak rośnie łączny majątek.

FUNKCJE PÓŹNIEJSZE (nice-to-have, po MVP):
- Dochody cykliczne dodawane automatycznie (np. co miesiąc wypłata).
- Wykresy/trendy (majątek w czasie, dochody vs wydatki).
- Kategorie własne i budżety miesięczne.
- Eksport danych (CSV).
- Konta użytkowników i synchronizacja w chmurze (patrz sekcja 6).
- Wersja mobilna (patrz sekcja 5).


========================================================================
5. PLATFORMA — DECYZJA
========================================================================
Kierunek: WEB NAJPIERW, POTEM MOBILE.
- MVP jako aplikacja webowa (działa w przeglądarce na komputerze
  i telefonie — responsywna, "mobile-friendly").
- Po dopracowaniu wersji web przenosimy/rozszerzamy na telefon.
- Projektujemy UI od razu z myślą o ekranie telefonu (mobile-first),
  żeby późniejsze przejście na mobile było łatwe.


========================================================================
6. ZAKRES MVP — DECYZJA
========================================================================
MVP: TRYB HYBRYDOWY — LOKALNIE LUB Z KONTEM.
- Bez logowania aplikacja nadal działa w pełni lokalnie i zapisuje dane
  w localStorage.
- Pierwsze wejście pokazuje bramę z logowaniem, rejestracją i świadomym
  wyborem trybu gościa. Wybór gościa jest zapamiętywany na urządzeniu.
- Nowy użytkownik lokalny i nowe konto zaczynają z pustym portfelem;
  dane demonstracyjne nie są już tworzone.
- Po zalogowaniu dane użytkownika są przechowywane w Supabase i chronione
  przez Row Level Security.
- Przy pierwszym logowaniu lokalny portfel jest kopiowany do Supabase tylko
  wtedy, gdy wszystkie kolekcje konta w chmurze są puste.
- Dane lokalne nie są kasowane; po wylogowaniu lokalny portfel jest ponownie
  dostępny. Dwa niepuste portfele nie są automatycznie scalane.


========================================================================
7. TECHNOLOGIA — REKOMENDACJA
========================================================================
(Dobrana pod prostotę i dobrą współpracę z Claude Code. Do potwierdzenia.)

MVP (web, tryb hybrydowy):
- Frontend: Next.js (React) + TypeScript.
- Stylowanie: Tailwind CSS (szybko i ładnie, spójny wygląd).
- Komponenty UI: shadcn/ui (gotowe, estetyczne elementy).
- Wykresy: Recharts.
- Dane lokalne: localStorage.
- Konta i chmura: Supabase Auth + Postgres z RLS.
- Testy jednostkowe: Vitest.

Etap 3 (mobile):
- React Native / Expo (można współdzielić dużo logiki z web),
  lub PWA jeśli wystarczy.

Uwaga: to rekomendacja, nie sztywna decyzja. Jeśli zmienimy stack,
zaktualizuj tę sekcję.


========================================================================
8. ZASADY UX / WYGLĄD
========================================================================
- Prostota ponad wszystko: mało kliknięć, żeby dodać dochód/wydatek.
- Ładny, nowoczesny, czysty wygląd. Przyjazny również dla dziecka.
- Duże, czytelne liczby (saldo, postęp celu).
- Szybkie dodawanie: jeden przycisk "+" zawsze pod ręką.
- Pozytywny ton (motywowanie do oszczędzania, paski postępu, "brawo").
- Brak żargonu — "ile mam", "ile wpłynęło", "ile wydałem", "ile do celu".


========================================================================
9. MODEL DANYCH (wstępny szkic)
========================================================================
Transaction (transakcja):
  id, typ ("income" | "expense"), kwota, waluta, data,
  kategoria, źródło/opis, cykliczna? (bool), notatka.

Goal (cel):
  id, nazwa, kwotaDocelowa, kwotaZebrana, link (opcjonalnie),
  zdjęcie (opcjonalnie), dataUtworzenia, status.

Asset (aktywo):
  id, nazwa, typ (gotówka/oszczędności/sprzęt/inwestycja),
  wartość, dataAktualizacji, notatka.

Category (kategoria):
  id, nazwa, typ (income/expense), ikona/kolor.

(Do rozbudowy przy implementacji.)


========================================================================
10. OTWARTE PYTANIA / DO USTALENIA
========================================================================
- Waluta: tylko PLN na start, czy od razu wielowalutowość?
- Czy cele mają "podpinać" kwotę z konkretnego aktywa/oszczędności,
  czy to osobny licznik?
- Czy aktywa mają się aktualizować ręcznie, czy w przyszłości
  automatycznie (np. kurs akcji/krypto przez API)?
- Jak dokładnie ma wyglądać "link do produktu" w celu (sam link,
  czy pobieranie zdjęcia i ceny ze strony)?
- Nazwa i logo — czy "Finly" zostaje na stałe?
- Next.js jest przypięty do 14.2.15, dla którego npm zgłasza znane
  podatności (naprawione w nowszych wydaniach) — do aktualizacji
  w jednym z kolejnych etapów.


========================================================================
11. DZIENNIK WĄTKÓW (dopisuj nowe ustalenia tutaj)
========================================================================
[2026-07-17] Rozmowa założycielska. Ustalono: koncept aplikacji
  (osobisty portfel do organizacji finansów, nie do płatności),
  grupa docelowa (każdy, od dziecka po dorosłego), funkcje rdzeniowe
  (dochody, wydatki, cele, aktywa), platforma (web najpierw, potem
  mobile), zakres MVP (lokalnie, bez kont), stack (rekomendacja:
  Next.js + Tailwind + shadcn/ui + dane lokalne). Utworzono folder
  Finly na pulpicie i plan krok po kroku w Notion (strona "finly").
[2026-07-17] Etap 1 — fundament projektu (Claude Code). Utworzono
  strukturę app/, components/, lib/ na bazie konfiguracji z Etapu 0.
  Powstały: app/globals.css (Tailwind), app/layout.tsx (lang pl, font
  Inter z latin-ext, mobile-first kontener max-w-md), components/
  Header.tsx (logo "F" + Finly), components/BottomNav.tsx (Pulpit,
  Cele, Aktywa + wyróżniony przycisk "+", ikony lucide-react, na razie
  bez akcji), app/page.tsx (makieta pulpitu z danymi zerowymi: saldo,
  Wpłynęło/Wydałem, pusty stan transakcji), lib/utils.ts (helper cn
  pod shadcn/ui). Kolor przewodni: emerald (brand w tailwind.config).
  npm install + npm run dev zweryfikowane — aplikacja startuje bez
  błędów pod http://localhost:3000.
[2026-07-17] Etap 2 — interaktywne demo (Claude Code). Aplikacja jest
  teraz w pełni klikalna: przycisk "+" otwiera formularz dodawania
  transakcji (dochód/wydatek, kwota, tytuł, kategoria, data, notatka),
  transakcje na pulpicie można kliknąć i zobaczyć szczegóły (typ, data,
  kategoria, notatka "dla kogo/na co") oraz usunąć. Nowe podstrony:
  /cele (paski postępu, wpłaty na cel, dodawanie/usuwanie celów,
  komunikat "Brawo!" po osiągnięciu) i /aktywa (suma majątku,
  aktualizacja wartości, dodawanie/usuwanie aktywów). Dolna nawigacja
  działa jako prawdziwe linki z podświetleniem aktywnej zakładki.
  Dane trzymane w localStorage (klucz "finly-data-v1") przez
  FinlyProvider (lib/store.tsx); przy pierwszym uruchomieniu ładowane
  są dane demo (m.in. transakcja "Prezent dla mamy", cele iPad
  i wakacje, aktywa: gotówka, konto, rower). Nowe pliki: lib/types.ts,
  lib/store.tsx, components/Modal.tsx, components/AddTransactionSheet.tsx,
  components/TransactionList.tsx, app/cele/page.tsx, app/aktywa/page.tsx.
  Kwoty i daty formatowane po polsku (Intl, lib/utils.ts).
[2026-07-17] Redesign "klockowy" (Claude Code). Nowy język wizualny
  w stylu zabawkowych klocków (à la Lego/Duolingo): każda karta
  i przycisk to "klocek" — grube obrysy (border-2, kolor ink #0c3529),
  twardy cień 0 4px 0 (shadow-brick) i animacja wciskania przy
  kliknięciu (klasy .brick / .brick-press w globals.css). Sygnatura:
  wypustki Lego (components/Studs.tsx) na dużych kafelkach salda
  (szmaragdowy) i majątku (żółty "sun" #ffc94d). Tło: mięta (mint),
  wnętrze "ramki telefonu" paper z pionowymi obrysami. Typografia:
  Baloo 2 (nagłówki i kwoty, --font-baloo) + Nunito (tekst,
  --font-nunito), obie z latin-ext. Nowe tokeny w tailwind.config:
  kolory ink/paper/mint/sun, cienie brick/brick-sm/brick-lg, fonty
  sans/display. Inter zastąpiony przez Nunito.
[2026-07-17] Pulpit bez listy transakcji (Claude Code). Feedback:
  lista transakcji wyglądała "jak bank". Usunięto components/
  TransactionList.tsx; zamiast niej na pulpicie jest dashboard
  wydatków (components/ExpenseDashboard.tsx): wszystkie wydatki
  pogrupowane po kategoriach, kolorowe paski udziału (szerokość
  względem największej kategorii, podpis = % wszystkich wydatków),
  suma "razem" w nagłówku. Kliknięcie kategorii otwiera modal ze
  szczegółami: suma i lista wydatków tej kategorii (tytuł, data,
  notatka, kwota) z możliwością usunięcia pojedynczego wydatku.
  Dane transakcji w store bez zmian (saldo i kafelki miesiąca
  nadal liczone z pełnej listy).
[2026-07-17] Dashboard w stylu Stripe/Shopify + fundament pod konta
  + logo (Claude Code). Pulpit: kafelki Wpłynęło/Wydałem mają teraz
  delty % vs poprzedni miesiąc (pigułki ze strzałką, zielone=dobrze,
  różowe=gorzej), nowy wykres wydatków z 30 dni (Recharts, cienka
  szmaragdowa linia z gradientem, tooltip, oszczędne osie —
  components/ExpenseChart.tsx; recharts dodany do dependencies),
  paski kategorii ujednolicone do koloru marki. Dane demo rozszerzone
  o poprzedni miesiąc (t5-t8), żeby delty i wykres miały co pokazywać
  (widoczne po wyczyszczeniu klucza finly-data-v1). Fundament pod
  bazę/logowanie: lib/storage.ts (interfejs DataStore + adapter
  localStorage; docelowo drugi adapter Supabase), lib/auth.tsx
  (AuthProvider/useAuth — zaślepka, user=null), typy FinlyData i User
  w lib/types.ts, .env.example z placeholderami Supabase. W nagłówku
  przycisk konta z modalem "Grasz lokalnie" i nieaktywnym "Zaloguj
  się (wkrótce)". Logo wygenerowane przez Higgsfield (nano banana):
  zielony klocek 2x2 z żółtą monetą, public/logo.png, użyte w Header.
[2026-07-17] Przełącznik dashboardów (Claude Code). Na pulpicie
  segmentowany przełącznik Wydatki/Dochody — przestawia jednocześnie
  wykres 30 dni i podział na kategorie ("Na co wydaję" / "Skąd mam
  pieniądze"). ExpenseChart i ExpenseDashboard uogólnione do
  TransactionChart i CategoryDashboard (prop type). Kolor podąża za
  typem w całej aplikacji: dochody emerald, wydatki rose (linia
  wykresu, paski kategorii, kwoty, aktywny przycisk przełącznika).
[2026-07-17] Etap kont i chmury — tryb hybrydowy. Dodano Supabase Auth,
  formularze logowania/rejestracji/wylogowania, schemat SQL z RLS oraz
  zapis transakcji, celów i aktywów w Postgres. Bez konta Finly nadal
  używa localStorage i danych demo. Pierwsze logowanie kopiuje lokalny
  portfel wyłącznie do całkowicie pustego konta chmurowego; lokalna kopia
  pozostaje bez zmian. Operacje chmurowe sprawdzają błędy przed zmianą UI.
  Dodano Vitest i testy reguł migracji, mapowania, auth oraz adaptera chmury.
[2026-07-17] Brama wejścia i pusty start. Nowy odwiedzający przed pulpitem
  wybiera logowanie, rejestrację albo „Kontynuuj bez logowania”. Tryb gościa
  jest zapamiętywany w localStorage, lecz wylogowanie wraca do bramy.
  Usunięto automatyczne dane demo: nowa przeglądarka i nowe konto zaczynają
  z pustymi transakcjami, celami i aktywami. Istniejący lokalny zapis jest
  zachowany, a rzeczywiste dane gościa nadal migrują wyłącznie do całkowicie
  pustego konta Supabase.
[2026-07-18] Poprawki z komentarzy Vercel/GitHub (issues #6–#11, Claude Code).
  Bez zmian w schemacie bazy — wszystko liczone po stronie klienta.
  • #8 Cele a saldo: „Mam” = zrealizowane dochody − wydatki − suma wpłat na
    cele (savedInGoals). Wpłata na cel realnie pomniejsza wolne środki;
    pod kwotą salda pigułka „🎯 na cele X”. Usunięcie/zmniejszenie celu
    przywraca środki (cele zostają osobną listą, nie tworzą wydatków).
  • #7 + #10 Oczekujące i daty z wyprzedzeniem: dochód z datą > dziś liczy
    się jako „Oczekujące” (druga, ciemna pigułka na kaflu salda) i wchodzi
    do „Mam” automatycznie, gdy nadejdzie jego data (porównanie ISO dat).
    Formularz pokazuje podpowiedź przy przyszłej dacie dochodu.
  • #6 Okres: przełącznik Miesiąc/Rok/Wszystko pod Wydatki/Dochody; filtruje
    wykres i podział na kategorie. Helpery todayISO/matchesPeriod/Period
    w lib/utils.ts; TransactionChart buduje serie dzienne (miesiąc),
    miesięczne (rok) lub od pierwszej transakcji (wszystko).
  • #11 Kategorie: dodano „Zdrowie” 🩺 do wydatków oraz opcję „➕ Stwórz
    własną…” (pole tekstowe). Własne kategorie są pamiętane, bo lista
    rozwijana łączy wbudowane z już użytymi w transakcjach danego typu.
  • #9 Niebieska obwódka: w globals.css warstwa base zdejmuje domyślny
    niebieski outline przeglądarki (:focus) i tap-highlight; dla klawiatury
    zostaje markowy zielony pierścień (:focus-visible, #059669).
[2026-07-18] Poprawki v2 po feedbacku (issues #6, #7 doprecyzowane).
  • #6 REALNE transakcje cykliczne (wcześniej błędnie zrobiony przełącznik
    widoku): nowe pole recurrence ("monthly"/"yearly") na Transaction,
    selektor „Jednorazowo / Co miesiąc / Co rok" w formularzu. Wystąpienia
    rozwijane helperem lib/recurrence.ts (occurrencesInRange + addPeriod,
    z przycięciem dnia do długości miesiąca) — używane w saldzie,
    oczekujących, wykresie i podziale na kategorie. Testy: lib/recurrence.test.ts.
    Baza: kolumna transactions.recurrence + migracja supabase/migrations/
    2026-07-18-add-recurrence.sql (tryb lokalny nie wymaga migracji);
    mapowanie w hybrid-data (recurrence + is_recurring).
  • #7 „Oczekujące" przeniesione OBOK „Mam" jako mniejsza, ciemniejsza
    rubryka (bg-ink/35). Liczy przyszłe przychody − przyszłe wydatki
    (zaplanowane + cykliczne) od jutra do końca bieżącego roku.
  • #8 potwierdzone: saldo = zrealizowane dochody − wydatki − odłożone na
    cele (teraz liczone z wystąpień, więc cykliczne też wchodzą).
  • #11 kategorie jako widoczne „klocki" do wyboru (zamiast dropdowna),
    z „Zdrowie" 🩺 i kafelkiem „➕ Własna" (pole na własną nazwę).
  • Kafle Wpłynęło/Wydałem i wykres liczą teraz wystąpienia (cykliczne
    widoczne w każdym miesiącu). Usuwanie cyklicznej pozycji kasuje całą serię.
[2026-07-18] UI: powrót do list rozwijanych (issues #14, #15). Feedback:
  „3 klocki obok = za dużo się dzieje, wybieraj z listy”.
  • #14 przełącznik okresu (Miesiąc/Rok/Wszystko) z trzech przycisków →
    kompaktowy <select> w nagłówku wykresu.
  • #15 wybór kategorii z „klocków” → z powrotem <select> (Zdrowie +
    „➕ Stwórz własną…”). Przy okazji selektor powtarzalności też jako
    <select> (spójność, mniej klikania). Logika cykliczności bez zmian.
[2026-07-18] Tryb rodzic↔dziecko (nowa funkcja; spec: docs/superpowers/
  specs/2026-07-18-parent-child-mode-design.md). Dziecko ma własne konto;
  rodzic łączy się jednorazowym kodem generowanym przez dziecko (ważny 15 min).
  Uprawnienia rodzica: podgląd całości (odczyt), dorzucanie kieszonkowego,
  dopłata do celu — bez edycji/kasowania wpisów dziecka. Dopłata do celu =
  wpływ +kwota ORAZ przeksięgowanie na cel, więc saldo „Mam” dziecka bez
  zmian, a pasek celu rośnie. UI: osobna zakładka „Dzieci” (/dzieci) u rodzica
  + sekcja „Rodzice” w modalu konta dziecka; wpisy od rodzica oznaczone
  „💛 od rodzica”. Technika: RLS + funkcje SECURITY DEFINER (bez Edge
  Functions). Nowe: tabele family_links, link_codes; kolumna
  transactions.added_by_user_id; migracja supabase/migrations/
  2026-07-18-parent-child.sql (konto wymaga uruchomienia; gość nie). Kod:
  lib/family.ts, lib/finance.ts (wspólne walletTotals, cykliczność-świadome),
  components/FamilyChildPanel.tsx, components/ChildDetail.tsx,
  app/dzieci/page.tsx, zmiany w BottomNav (5 kolumn, zakładka Dzieci)
  i Header. Tryb rodzic↔dziecko wymaga konta (gość dostaje podpowiedź).
[2026-07-18] Wybór roli na wejściu (rodzic/dziecko). Ekran EntryGate zaczyna
  się od wyboru „Jestem dzieckiem” / „Jestem rodzicem” (z wyjaśnieniem), plus
  „Mam już konto — zaloguj się” i tryb gościa. Rola zapisywana przy rejestracji
  w user_metadata.role (Supabase), czytana do User.role w auth.tsx. UI wg roli:
  dziecko NIE widzi zakładki „Dzieci” (za to ma panel „Rodzice/pokaż kod”
  w koncie); rodzic widzi „Dzieci”, bez panelu „Rodzice”. Stare konta/gość bez
  roli widzą pełny interfejs (wstecznie zgodne). AuthForm: props lockMode+role;
  BottomNav filtruje zakładki wg roli (grid 4/5 kolumn); /dzieci ma zaporę dla
  konta dziecka. Testy EntryGate zaktualizowane (wybór roli + rola w signUp).
[2026-07-18] PWA — apka instalowalna i działająca offline. Dodano
  app/manifest.ts (Next generuje /manifest.webmanifest: nazwa, kolory
  theme #10b981 / tło mint, display standalone, ikony), ikony wygenerowane
  z logo 1024px: public/icon-192.png, icon-512.png, icon-maskable-512.png
  (maskable z ~14% paddingiem na białym tle). Service worker public/sw.js:
  nawigacje sieć-najpierw (offline → powłoka z cache), zasoby /_next/static
  cache-najpierw; zapytania do innych domen (Supabase) ignorowane; czyszczenie
  starych cache przy activate. Rejestracja tylko na produkcji przez
  components/ServiceWorkerRegister.tsx (nie koliduje z `next dev`/HMR).
  Metadane w layout: appleWebApp, apple-touch-icon, viewport.themeColor.
  Tryb gościa (localStorage) działa więc offline; tryb konta wymaga sieci.
