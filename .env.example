# Konfiguracja pod przyszły etap kont i bazy danych (Supabase).
# Skopiuj ten plik jako .env.local i uzupełnij wartości, gdy będziemy
# podłączać logowanie i synchronizację w chmurze.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
